import { CheckClient } from "./CheckClient";

export const metadata = {
  title: "check for vibe — solana_vibes",
  description: "Connect X to see if someone sent you a vibe.",
};

export default function CheckPage() {
  return <CheckClient />;
}
